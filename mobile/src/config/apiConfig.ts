/**
 * API base URL — must be reachable from the phone/emulator.
 *
 * - Physical device: use the server IP on the same Wi‑Fi/VPN (e.g. 10.2.0.121).
 * - Android emulator: use 10.0.2.2 to reach the host machine.
 * - Public IP only if the server is exposed on the internet.
 */
export const API_BASE_URL =
//'http://10.2.0.30:8182/dmCmsService/rest/endpoints';
  
'http://223.30.224.244:8182/dmCmsService/rest/endpoints';
